package com.test.logics;

public class StreamApi {

	public static void main(String[] args) {

		
		 //List<Double> salary=Arrays.asList(10000.0,30000.0,20000.0,40000.0,50000.0);
		 //salary.stream().map(num->num*1.10).forEach(System.out::println);
		
//		  List<String> list=Arrays.asList("Praveen","Mahesh");
//		  list.stream().filter(s->s.contains("M")).forEach(System.out::println);
//		 

//		List<String> list=new ArrayList<>();
//		Collections.addAll(list, "Praveen","Mahesh","Suguna");
//		List<String> upper=list.stream().map(name->name.toUpperCase()).collect(Collectors.toList());
//		
//		System.out.println(upper);
		
		/*
		 * Map<String, Long> resultMap =
		 * Arrays.stream(str.split(" ")).map(String::toLowerCase)
		 * .collect(Collectors.groupingBy(s -> s, LinkedHashMap::new,
		 * Collectors.counting())); System.out.println(resultMap);
		 */
//			char[] ch=str.toCharArray();
//			
//				List<String> name=Arrays.stream(str.split(""))
//						.collect(Collectors.groupingBy( Function.identity(),Collectors.counting()))
//						.entrySet().stream()
//						.filter(num -> num.getValue()>1)
//						.map(Map.Entry::getKey)
//						.collect(Collectors.toList());
//				
//					System.out.println(name);
			

//		Scanner sc=new Scanner(System.in);
//		System.out.println("Enter A String");
//		String str=sc.next();
//		
//		
//		Map<String, Long> input=Arrays.stream(str.split("")).map(String::toUpperCase)
//				.collect(Collectors.groupingBy(s->s,LinkedHashMap::new,Collectors.counting()));
//		System.out.println(input);
//		
		
		
		
//		List<String> result=Arrays.asList("Praveen","Mahesh","Smile");
//		result.stream().sorted().forEach(System.out::println);
		
		
//		List<String> list1=new ArrayList<>();
//		Collections.addAll(list1, "Praveen","Mahesh","Suguna");
//		
//		list1.stream().map(name->name.length()).sorted().forEach(System.out::println);
	
		
		
		
		
	}

}
